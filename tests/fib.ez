External function PutNum(n).
external function PutChar(n).

Function fib(a),
    if a <= 2,
        return 1.
    !
    return fib(a - 1) + fib(a - 2).
!

set result to fib(10).
set tmp to PutNum(result).
