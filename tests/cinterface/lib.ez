[ Function another_test(a, b, c, d, e, f,g), ]
[   return g. ]
[ ! ]

Function fib(n),
  set counter to 1.
  set a to 1.
  set b to 0.
  [start the loop]
  loop,
    change a to a + b.
    change counter to counter + 1.
    if counter > n,
      return a.
    !
    change b to a + b.
    change counter to counter + 1.
    if counter > n,
      return b.
    !
  !
!
